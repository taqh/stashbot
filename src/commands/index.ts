import { command as pingCommand } from './ping';
import { command as promptCommand } from './prompt';
import { command as helpCommand } from './help';

export const commands = [pingCommand, promptCommand, helpCommand];
